var buttonClick = function (){

    var name = 'visug';
    // Notice how JavaScript is dynamically typed. 
    // This will only break at runtime, there is no 'compile time'.
    name = 5;
    alert('hello ' + name.toUpperCase());

};